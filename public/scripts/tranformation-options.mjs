export default class TransformationOptions {
    constructor(type, property, sortDirection) {
        this.type = type;
        this.property = property;
        this.sortDirection = sortDirection;
    }
}
